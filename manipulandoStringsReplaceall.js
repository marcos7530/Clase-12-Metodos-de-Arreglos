const mensaje = `Leavemealone`;

//El método replaceAll me devuelve una nueva cadena de caracteres 
//donde se reemplazaron todas las subcadenas especificadas por una 
//subcadena que proporcionamos 

const nuevoMensaje= mensaje.replaceAll(`e`,`🤔`);

console.log(mensaje);
console.log(nuevoMensaje);
